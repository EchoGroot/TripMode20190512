<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC  "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="getLocation">
    <select id="getLocation" resultType="TripMode.pojo.TheCrossing" parameterType="String">
        select * from tripmode_crossing where ID = {#}
    </select>
</mapper>